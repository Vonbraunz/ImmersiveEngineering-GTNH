package mcp.mobius.waila.api;

public class IWailaRegistrar {

    public void registerBodyProvider(IWailaDataProvider dataProvider, Class<?> blockOrTileClass) {
    }

    public void registerHeadProvider(IWailaDataProvider dataProvider, Class<?> blockOrTileClass) {
    }

    public void registerTailProvider(IWailaDataProvider dataProvider, Class<?> blockOrTileClass) {
    }

    public void registerStackProvider(IWailaDataProvider dataProvider, Class<?> blockOrTileClass) {
    }

    public void registerNBTProvider(IWailaDataProvider dataProvider, Class<?> blockOrTileClass) {
    }
}
